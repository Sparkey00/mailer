<form method="post" action="{{ path('app_recipient_delete', {'id': recipient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipient.id) }}">
    <button class="btn">Delete</button>
</form>
